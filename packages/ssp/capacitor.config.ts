import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ssp',
  webDir: '../../dist/packages/ssp',
  bundledWebRuntime: false,
};

export default config;
